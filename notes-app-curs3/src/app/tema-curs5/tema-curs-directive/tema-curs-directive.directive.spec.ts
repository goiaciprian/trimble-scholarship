import { TemaCursDirectiveDirective } from './tema-curs-directive.directive';

describe('TemaCursDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TemaCursDirectiveDirective(null);
    expect(directive).toBeTruthy();
  });
});
